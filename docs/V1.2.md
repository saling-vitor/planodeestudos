# Plano ARQ · Ciclo V1.2

Documento de abertura do ciclo posterior à V1.1.0.

## Baseline congelada

- produto publicado: **1.1.0**;
- SHA da baseline: `79da5bfe2f486da208b6e04a3fc06e13eae9a54d`;
- produção: `main`;
- branch congelada: `release/v1.1.0`;
- tag/GitHub Release: `v1.1.0`;
- deploy de produção validado pelo workflow **Publicar Plano ARQ #598**.

## Branch de desenvolvimento

Todo trabalho posterior à V1.1.0 deve ocorrer em:

`develop/v1.2`

Não alterar `release/v1.1.0` nem reescrever a tag `v1.1.0`.

## Estado inicial

A branch V1.2 foi aberta **sem funcionalidade nova**.

Ainda não estão autorizados:

- bump da versão de produto para 1.2.0;
- redesign;
- migração de storage;
- remoção de aliases/compatibilidade;
- alterações de mapas, PWA, sync ou automação sem escopo e critérios de aceite;
- limpeza destrutiva apenas por estética técnica.

## Regras herdadas

1. `main` representa produção estável.
2. Alterações começam em `develop/v1.2`.
3. Preservar dados, URLs, localStorage, backups, contestId e IDs/namespaces dos mapas.
4. Manter modelo local-first.
5. Mudança visual exige auditoria desktop + tablet/iPad + celular.
6. Mudança de dados exige Data Safety.
7. Mudança de PWA/cache exige cold-offline e teste no deploy real.
8. Mudança de mapas exige a matriz real dos materiais.
9. ALTERAR → SALVAR → VALIDAR → REGRESSÃO → CONTINUAR.
10. Nenhuma feature entra apenas para preencher versão.

## ETAPA W · Auditoria pós-V1.1.0 — CONCLUÍDA

A auditoria está registrada em:

- `docs/V1.2_AUDIT.md`;
- `docs/V1.2_BACKLOG.md`.

Resultado principal: não foi comprovado bug funcional novo na produção V1.1.0, mas a nova branch ainda não possui CI automático e o gate de desenvolvimento continua acoplado ao RC da V1.1.

## ETAPA X · Safety Rails V1.2 — CONCLUÍDA

Concluído em `develop/v1.2`:

1. CI automático para `develop/**` criado em `.github/workflows/validate-development.yml`;
2. validação contínua separada de RC/release;
3. contrato ativo da V1.1 fechado como `released`;
4. Gate V1.1 Release #4 e deploy #598 registrados no contrato;
5. README e documentação de release atualizados para a baseline pública 1.1.0;
6. primeiro **Validar desenvolvimento** executado com sucesso.

Nenhuma funcionalidade de produto foi alterada nesta etapa.

## ETAPA Y · Versionamento e CI sustentável — CONCLUÍDA

A infraestrutura de versão PWA foi consolidada sem alteração funcional:

1. `assets/js/pa-pwa-v01.js` tornou-se a única fonte do número-base PWA;
2. `scripts/sync_pwa_version.py` deriva o Service Worker;
3. o Offline Pack deriva sua versão da mesma fonte;
4. o workflow de Pages deriva a versão publicada usando o SHA;
5. `pages.yml` e `build_offline_pack.py` não contêm mais `19.4` hardcoded;
6. os validadores bloqueiam divergência ou retorno de hardcode;
7. o gate `Validar desenvolvimento` continua genérico para `develop/**`;
8. RC/QA da V1.1 permanecem como histórico separado do estado ativo.

**Validar desenvolvimento #4:** success.

## ETAPA Z · Manutenção de Mapas — fase 2 — CONCLUÍDA

Foi feita somente consolidação comprovadamente neutra:

- CSS compartilhado: **581.558 → 580.305 caracteres**;
- `@media`: **240 → 227**;
- 37 regras/grupos vazios removidos;
- 6 `@media` idênticos adjacentes fundidos;
- 2 regras adjacentes do mesmo seletor/contexto fundidas;
- `!important`: **9.341 → 9.341**, preservados conscientemente;
- novo auditor permanente para impedir retorno desses resíduos.

Os blocos responsivos repetidos que restam são **não adjacentes** e dependem da ordem da cascata. Não serão movidos ou agrupados sem um caso funcional concreto.

Validação:

- Validar desenvolvimento #6: **success**;
- Validar desenvolvimento #7: **success**;
- regressão dos mapas: **aprovada**.

## ETAPA AA · Modularização gradual do Portal — CONCLUÍDA

A análise de `index.html`, `configuracoes.html` e `biblioteca.html` encontrou **zero funções de corpo idêntico** entre as três páginas. Portanto, não foi criado um módulo genérico artificial.

Foi consolidado apenas o contrato realmente duplicado:

- Configurações e Biblioteca passaram a usar `pa-contest-context-v01.js` para a lista mesclada de concursos;
- o shell delega ao mesmo contexto quando ele existe, preservando fallback para páginas sem o módulo;
- `index.html` mantém sua rotina própria porque ela também migra e persiste dados do edital, não sendo equivalente ao contexto simples;
- criado `scripts/audit_portal_inline.py` como guardrail.

Redução de JS inline: **500 caracteres**, sem redesign ou alteração funcional.

Validação:

- Validar desenvolvimento #9: **success**;
- Validar desenvolvimento #10: **success**.

## ETAPA AB · Auditoria final e freeze da V1.2 — CONCLUÍDA

Auditoria contra `v1.1.0`:

- branch: **13 commits à frente / 0 atrás**;
- arquivos alterados: **21**;
- todas as alterações classificadas nas etapas W/X/Y/Z/AA;
- arquivos temporários/resíduos rastreados: **0**;
- `BUILD_MAINTENANCE=false`;
- `MAINTENANCE_MODE=false`;
- Validar desenvolvimento #11: **success** no HEAD funcional.

Functional freeze:

`84afc81faf45af6d67ee9e3441d5f1204734bccd`

A partir desse SHA, o feature freeze da V1.2 está ativo. Mudanças posteriores ficam restritas à allowlist de release/QA/CI/versionamento registrada em `data/release-freeze-v12.json`.

Foi criado `scripts/validate_release_freeze_v12.py`; o gate contínuo agora usa histórico Git completo e reprova alterações pós-freeze fora da allowlist.

## ETAPA AC · Release Candidate V1.2 — CONCLUÍDA

Release Candidate criado:

- branch: `release/v1.2.0-rc1`;
- candidate: `1.2.0-rc.1`;
- SHA do RC aprovado pelo primeiro gate: `9cdf4eb50d953c0631e3e2795f71f922fdcf0521`;
- Gate V1.2 RC #1: **success**;
- versão pública/runtime mantida em **1.1.0**;
- feature freeze preservado;
- nenhuma mudança funcional fora da allowlist.

O RC automatizado está aprovado. Permanecem pendentes somente os checks manuais de Chrome real em **80%**, **125%** e **150%**.

## Próxima etapa

**ETAPA AD · QA manual e Gate final V1.2**

1. validar Biblioteca, Configurações e um mapa real em 80%, 125% e 150%;
2. registrar evidências em `data/release-qa-v12.json`;
3. criar/ativar o Gate V1.2 Release;
4. exigir QA manual aprovado;
5. preparar a versão final 1.2.0 somente depois do gate final verde.

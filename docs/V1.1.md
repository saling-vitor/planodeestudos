# Plano ARQ · Ciclo V1.1

Documento operacional do ciclo posterior à V1.0.0.

## Baseline de origem

- V1.0.0 aprovada: `6249beebee753a5e51ed879fb8b7de924dee1a48`.
- Branch congelada: `release/v1.0.0`.
- Tag/GitHub Release `v1.0.0`: **publicada** e apontando para o mesmo SHA.
- A V1.0.0 permanece preservada como baseline histórica.

## Branch de desenvolvimento

Todo trabalho novo da V1.1 deve ocorrer em `develop/v1.1`.

Não alterar `release/v1.0.0` para implementar melhorias, experimentos ou correções da V1.1.

## Regras do ciclo

1. Preservar compatibilidade com dados da V1.0.0.
2. Não reintroduzir sidebar + bottom navigation simultâneas.
3. Não reintroduzir hamburger/drawer legado.
4. Não criar versões paralelas de shell, componentes ou navegação.
5. Alterações globais devem ser feitas na base compartilhada.
6. Cada mudança deve passar por ALTERAR → SALVAR → VALIDAR.
7. Mudanças que afetem layout devem passar pela auditoria desktop + iPad/tablet + celular.
8. Mudanças que afetem dados, concurso, edital, mapas ou planejamento devem repetir o fluxo operacional de novo concurso.
9. Mudanças em PWA/cache/offline devem passar pelo smoke test real de PWA.
10. Evitar quebra de URLs, localStorage, backups, contestId e identificadores dos mapas.

## Gate obrigatório para futura V1.1.0

Antes de promover a V1.1.0:

- validadores estáticos sem erros;
- auditoria visual/funcional sem regressões;
- fluxo de novo concurso aprovado;
- smoke PWA aprovado;
- ausência de temporários, backups e legado comprovadamente morto;
- documentação e release notes atualizadas;
- commit final congelado antes da tag.

## Pendências herdadas — CONCLUÍDAS

- tag/release oficial `v1.0.0`: **publicada**;
- zoom real do Chrome em 80%, 125% e 150%: **aprovado**.

## Escopo funcional da V1.1

O escopo auditado está em `docs/V1.1_BACKLOG.md`.

A ordem aprovada para o ciclo começa por segurança dos dados e sincronização, seguida pela consolidação/auditoria dos mapas de estudo, performance, confiabilidade offline, observabilidade e somente depois evolução da automação interna.

Nenhuma funcionalidade nova deve ser adicionada apenas para “encher” a V1.1.


## Release Candidate 1 — APROVADO

A V1.1 entrou em **feature freeze** após o commit funcional:

`db357f7fed6a7cdf8ac12c2c08c8a60c80156517`

Candidato preparado:

- versão: **1.1.0-rc.1**;
- branch reservada: `release/v1.1.0-rc1`;
- manifesto: `data/release-candidate-v11.json`;
- release notes: `docs/V1.1_RELEASE_NOTES.md`;
- gate automático: **Gate V1.1 RC**.

Depois do feature freeze, somente documentação, metadados de release, QA, validadores e workflows explicitamente listados no manifesto podem mudar.

Qualquer correção que altere runtime, CSS funcional, HTML funcional, dados de aplicação ou comportamento exige:

1. retornar à `develop/v1.1`;
2. corrigir;
3. repetir o gate completo;
4. mover o `functionalFreezeSha`;
5. gerar um novo RC.

O RC1 passou pelo QA manual em **80% / 125% / 150%** e pelo **Gate V1.1 Release #1** no SHA `ecae8ef1927c8ea8a1e5086a77959dfc15a0e34d`.

## Release final V1.1.0 — PUBLICADA

Branch final: `release/v1.1.0`.

SHA final congelado:

`79da5bfe2f486da208b6e04a3fc06e13eae9a54d`

Estado final:

- Gate V1.1 Release #4: **success**;
- `main`: promovido para o SHA final;
- GitHub Pages / workflow Publicar Plano ARQ #598: **success**;
- auditoria visual do deploy: **success**;
- fluxo operacional de concurso novo no deploy: **success**;
- teste PWA no deploy real: **success**;
- tag/GitHub Release `v1.1.0`: **publicada**;
- `main`, `release/v1.1.0` e `v1.1.0`: mesmo SHA final.

A V1.1.0 está encerrada e não recebe novas alterações. Trabalho posterior parte de `develop/v1.2`.
